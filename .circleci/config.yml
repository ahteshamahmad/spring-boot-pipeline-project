version: 2.1

workflows:
  build-workflow:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: ahteshamahmad/linux_machine
    steps:
      - run: echo "Hi I'm on Runners!"
