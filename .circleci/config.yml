version: 2.1
jobs:
  build1:
    docker:
      - image: circleci/openjdk:11
    steps:
      - checkout
      - run: echo "Hello world"
  build2:
    steps:
      - checkout
      - run: echo "Testing"
workflows:
  jobs:
    - build1
    - build2
